{
  "name": "uinames",
  "description": "A simple module to generate random localized names using uinames(http://uinames.com) as the source",
  "version": "0.0.3",
  "dependencies": {    
    "defaults": "^1.0.0",
    "request": "^2.51.0"
  },
  "scripts": {
    "lint": "jshint .",
    "update-source": "node getsource.js update",
    "get-source": "node getsource.js create",
    "pretest": "npm run-script lint",
    "test": "istanbul cover ./node_modules/.bin/_mocha test",
    "posttest": "istanbul check-coverage && rm -rf coverage",
    "prepublish": "npm test && npm prune",
    "postinstall": "node getsource.js create"
  },
  "publishConfig": {
    "registry": "http://registry.npmjs.org"
  },
  "author": "Adrián Estrada <edsadr@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "mocha": "^1.21.4",
    "chai": "^1.10.0",
    "istanbul": "^0.3.2",
    "jshint": "^2.5.6"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:edsadr/uinames.git"
  }
}
